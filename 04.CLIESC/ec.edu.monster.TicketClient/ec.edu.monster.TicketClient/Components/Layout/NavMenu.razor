@using ec.edu.monster.TicketClient.State
@inject CarritoState Carrito

<div class="nav-inner">
    <a href="/" class="nav-brand">
        <svg width="28" height="28" viewBox="0 0 32 32" fill="none">
            <circle cx="16" cy="16" r="14" stroke="#C8A951" stroke-width="2" fill="none"/>
            <path d="M16 4 L16 28 M4 16 L28 16 M8 8 L24 24 M24 8 L8 24" stroke="#C8A951" stroke-width="0.6" opacity="0.4"/>
            <circle cx="16" cy="16" r="4" fill="#C8A951"/>
        </svg>
        <span class="nav-brand-text">TicketPremium</span>
    </a>

    <nav class="nav-links" aria-label="Main navigation">
        <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
            Inicio
        </NavLink>

        <NavLink class="nav-link" href="partidos">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
            Partidos
        </NavLink>

        <NavLink class="nav-link cart-link" href="facturacion">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
            Carrito
            @if (Carrito.AsientosSeleccionados.Count > 0)
            {
                <span class="cart-badge">@Carrito.AsientosSeleccionados.Count</span>
            }
        </NavLink>
    </nav>
</div>

@code {
    protected override void OnInitialized()
    {
        Carrito.OnChange += StateHasChanged;
    }

    public void Dispose()
    {
        Carrito.OnChange -= StateHasChanged;
    }
}
